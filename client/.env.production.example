# Copy to .env.production before `pnpm build` / `npm run build`.
# Vite inlines VITE_* at build time — changing this requires a new client deploy.

# HTTPS URL of your Nest API (no trailing slash)
VITE_API_URL=https://api.your-domain.com
